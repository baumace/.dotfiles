1. Add automated USB attach and detach
2. Ensure that screen recording works as well as camera and microphone
3. Fix waybar backlight icons — install `ttf-nerd-fonts-symbols` (`sudo pacman -S ttf-nerd-fonts-symbols`) and add `"Symbols Nerd Font"` to the front of the `font-family` in `waybar/style.css`
