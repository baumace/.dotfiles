# hypridle — idle management for Hyprland

general {
    lock_cmd = pidof hyprlock || hyprlock       # don't spawn a second instance if already locked
    before_sleep_cmd = pidof hyprlock || hyprlock
    after_sleep_cmd = hyprctl dispatch dpms on
}

